# univ= 'inha university'
# counts_alphabet = {letter : univ.count(letter) for letter in univ}
# print(counts_alphabet)

# univ= 'inha university'
# counts_alphabet = dict()
# for i in univ:
#     counts_alphabet[i] = univ.count(i)
# print(counts_alphabet)

#assignment
squares = {n : n**2 for n in range(10)} #n*n = pow(n,2) = n**2
print(squares)